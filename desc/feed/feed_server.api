syntax = "v1"

info(
	title: "短视频Feed"
	desc: "主页短视频Feed"
	author: "caicandong"
	email: "caicandong@shu.edu.cn"
)

import (
	"feed.api"
)

//============================> usercenter v1 <============================
//no need login
@server(
	prefix: api/v1
	group: feed
)
service main {
	@doc "register"
	@handler feed
	get /feed (FeedReq) returns (FeedResp)
}
