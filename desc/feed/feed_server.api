syntax = "v1"

info(
	title: "主页Feed"
	desc: "主页Feed"
	version: "v1"
)

import (
	"feed.api"
)

//============================> usercenter v1 <============================
//no need login
@server(
	prefix: api/v1
	group: feed
)
service main {
	@doc "register"
	@handler feed
	get /feed (FeedReq) returns (FeedResp)
}
